<Window x:Class="SJ_CADtoRevit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SJ_CADtoRevit" Height="460" Width="380"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI, Malgun Gothic">
    <Window.Resources>
        <!-- Custom Button Style -->
        <Style x:Key="RunButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#0052D4" Offset="0"/>
                        <GradientStop Color="#00B4DB" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                SnapsToDevicePixels="true">
                            <Border.Effect>
                                <DropShadowEffect Color="#00D2FF" BlurRadius="6" ShadowDepth="1" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#0043B4" Offset="0"/>
                                            <GradientStop Color="#009EC3" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#003185" Offset="0"/>
                                            <GradientStop Color="#007E9C" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderThickness="1.5" BorderBrush="#00D2FF" CornerRadius="8" Background="#1E1E24" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Background="#141419" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
                    <TextBlock Text="SJ_CADtoRevit" 
                               Foreground="#00D2FF" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               VerticalAlignment="Center"/>
                    <TextBlock Text=" - Revit Crop &amp; Clean" 
                               Foreground="#8E8E9F" 
                               FontSize="11.5" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Close Button -->
                <Button Grid.Column="1" 
                        x:Name="btnClose" 
                        Content="✕" 
                        Foreground="#6D6D7C" 
                        Background="Transparent" 
                        BorderThickness="0" 
                        FontSize="16" 
                        Cursor="Hand"
                        Click="btnClose_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bg" Background="Transparent">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="bg" Property="Background" Value="#E81123"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Execution Log Terminal -->
            <Grid Grid.Row="1" Margin="15,15,15,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" 
                           Text="작업 진행 로그" 
                           Foreground="#E2E2E6" 
                           FontSize="12" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,5"/>
                <Border Grid.Row="1" 
                        Background="#101014" 
                        BorderBrush="#2D2D37" 
                        BorderThickness="1" 
                        CornerRadius="5" 
                        Padding="8">
                    <TextBox x:Name="txtLog" 
                             Background="Transparent" 
                             BorderThickness="0" 
                             Foreground="#A3E281" 
                             FontFamily="Consolas, Courier New" 
                             FontSize="11" 
                             IsReadOnly="True" 
                             VerticalScrollBarVisibility="Auto" 
                             HorizontalScrollBarVisibility="Auto" 
                             TextWrapping="Wrap"
                             Text="[대기 중] 경계선을 지정한 후 작업을 실행하십시오."/>
                </Border>
            </Grid>

            <!-- Run Action -->
            <Grid Grid.Row="2" Margin="15,0,15,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button x:Name="btnRunSingle" 
                        Grid.Row="0" Grid.Column="0"
                        Content="선택 블록만 자르기" 
                        Style="{StaticResource RunButton}" 
                        Margin="0,0,5,5"
                        Click="btnRunSingle_Click"/>
                
                <Button x:Name="btnRunSelection" 
                        Grid.Row="0" Grid.Column="1"
                        Content="선택 영역만 자르기" 
                        Style="{StaticResource RunButton}" 
                        Margin="5,0,0,5"
                        Click="btnRunSelection_Click"/>
                
                <Button x:Name="btnRunAll" 
                        Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                        Content="전체 도면 자르기" 
                        Style="{StaticResource RunButton}" 
                        Margin="0,5,0,5"
                        Click="btnRunAll_Click"/>

                <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
                           Text="* 닫힌 곡선(폴리선, 원 등)을 경계선으로 선택하세요." 
                           Foreground="#6D6D7C" 
                           FontSize="10.5" 
                           HorizontalAlignment="Center" 
                           Margin="0,8,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
